#include <stdio.h>
/**
 * main - Entry point
 * Description: 'Prints alphabet in lowercase and then uppercase'
 * Return: Always 0  (Success)
 */
int main(void)
{
	int n = 97;
	int n = 65;

	while (n <= 122)
	{
		putchar(n);
	}
	while (n <= 90)
	{
		putchar(n);
	}
	putchar('\n')
	return (0);
}

